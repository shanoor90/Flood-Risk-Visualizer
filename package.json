{
    "name": "flood-risk-visualizer-root",
    "version": "1.0.0",
    "description": "Root package for Flood Risk Visualizer",
    "scripts": {
        "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
        "start:backend": "cd backend && npm run dev",
        "start:frontend": "cd frontend && npx expo start --go",
        "start:all": "npx concurrently \"npm run start:backend\" \"npm run start:frontend\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}